# Render env vars
# Google Drive direct download URL for the pickle
MODEL_URL=
# Optional: if you bake the model file into the repo or Render disk
# MODEL_PATH=/path/to/disease_prediction_catboost_hybrid.pkl

# If the pickle doesn't contain the symptom vocabulary, provide it here:
# SYMPTOMS_JSON=["fever","cough","headache"]

# Optional: where to cache the downloaded model (defaults to temp dir)
# MODEL_CACHE_DIR=/tmp

# Optional: force the downloaded filename
# MODEL_FILENAME=disease_prediction_catboost_hybrid.pkl

# Allow your frontend domain(s)
CORS_ORIGIN=http://localhost:5173,https://care-sentry-ai.vercel.app

# How many predictions to return
TOP_N=5
